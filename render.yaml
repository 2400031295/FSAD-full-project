services:
  - type: web
    name: assignment-hub-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index-simple.js
    healthCheckPath: /api/status
    envVars:
      - key: NODE_ENV
        value: production
